## feat: 초기 워크스페이스 및 하네스 세팅

- Rust 워크스페이스 구조 생성 (common, disk, memory, network, windows)
- CLAUDE.md 하네스 설정 추가
- .gitignore 민감 파일 제외 설정
- docs/, tests/fixtures/ 디렉토리 구조 추가

## feat: disk 모듈 Phase 1 - common 유틸 + 이미지 정보

- common: hash(MD5+SHA256 단일 패스), logging(tracing), timestamp(FAT32/Unix) 구현
- disk: error, image 모듈 구현 + CLI 스켈레톤 (info/partitions/files/deleted)
- 테스트 5개, clippy 경고 0개

## feat: disk 모듈 Phase 2 - MBR/GPT 파티션 파싱

- MBR 파티션 파싱 (byteorder, 16가지 타입 이름 매핑)
- GPT 파티션 파싱 (gpt 크레이트)
- 자동 감지 (0x55AA + Protective MBR 0xEE)
- 테스트 7개, clippy 경고 0개

## feat: disk 모듈 Phase 3,4 - FAT32 탐색 + 삭제 파일 탐지

- filesystem.rs: PartitionSlice 래퍼 + fatfs 크레이트로 FAT32 재귀 탐색
- deleted.rs: BPB 파싱 + 0xE5 마커 raw 스캔으로 삭제 파일 탐지
- 복구 가능성 판단 (first_cluster > 1 && size > 0)
- 테스트 11개, clippy 경고 0개
